﻿# JainFood Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env files with real secrets!
# ===========================================
# ENVIRONMENT
# ===========================================
APP_ENV=dev
GIN_MODE=debug
PORT=8080
# ===========================================
# DATABASE (PostgreSQL)
# ===========================================
# For Docker Compose (required for local development)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_set_your_password
POSTGRES_DB=jain_food
# Full connection URL (used by the API)
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?sslmode=disable
# ===========================================
# REDIS
# ===========================================
REDIS_ADDR=localhost:6379
# REDIS_URL=rediss://default:xxx@xxx.upstash.io:6379
# ===========================================
# SECURITY (Generate random strings!)
# ===========================================
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_32_CHARS
OTP_SECRET=CHANGE_ME_GENERATE_RANDOM_32_CHARS
# ===========================================
# OBJECT STORAGE (S3/MinIO)
# ===========================================
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=changeme_minio_access_key
S3_SECRET_KEY=changeme_minio_secret_key
S3_BUCKET=jain-food-media
# ===========================================
# SMS SERVICE
# ===========================================
NOTIFY_SERVICE=console
# SMSINDIAHUB_API_KEY=your_api_key
# SMSINDIAHUB_SENDER_ID=JAINFO
# ===========================================
# PAYMENT (Razorpay)
# ===========================================
# RAZORPAY_KEY_ID=rzp_test_xxx
# RAZORPAY_KEY_SECRET=xxx
